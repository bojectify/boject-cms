node_modules
*.log
# Ignore transient sampler output; real sampler output lives under reports/
/tmp-samples/

# Reports are committed deliverables (summary.md / metrics.csv / plots/*.png).
# Only the raw NDJSON is ignored — k6 emits 10s of MB per scenario, far too
# noisy for review.
/reports/**/raw*.json
