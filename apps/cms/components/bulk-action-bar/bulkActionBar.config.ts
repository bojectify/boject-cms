import { testIds } from '~/utils/test-config/testConfig.utils';

export const QA_BULK_ACTION_BAR = {
  ...testIds('BULK_ACTION_BAR', {
    COUNT: 'count',
    PUBLISH_BUTTON: 'publish-button',
    CLEAR_BUTTON: 'clear-button',
  }),
};
