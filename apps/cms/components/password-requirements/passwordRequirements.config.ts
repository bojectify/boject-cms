import { testIds } from '~/utils/test-config/testConfig.utils';

export const QA_PASSWORD_REQUIREMENTS = {
  ...testIds('PASSWORD_REQUIREMENTS', {
    RULE: 'rule',
    RULE_ICON: 'rule-icon',
    RULE_LABEL: 'rule-label',
  }),
};
