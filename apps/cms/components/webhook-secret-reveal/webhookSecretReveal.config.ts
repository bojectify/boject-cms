import { testIds } from '~/utils/test-config/testConfig.utils';

export const QA_WEBHOOK_SECRET_REVEAL = {
  ...testIds('WEBHOOK_SECRET_REVEAL', {
    CONTAINER: 'container',
    SECRET: 'secret',
    COPY: 'copy',
  }),
};
